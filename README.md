# claude-code-sandbox

A tiny sandbox repo for practicing the GitHub PR workflow with Claude Code.

## What is this?

This repo exists purely as a plaground for trying out branches, commits,
and pull requests without touching real project code.

## Usage

Clone it, make a small change on a branch, and open a PR against `main`.
